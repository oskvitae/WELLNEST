<h1>Services#index</h1>
<p>Find me in app/views/services/index.html.erb</p>

<%= form_with(url: service_path, method: :get, local: true) do %>
  <%= label_tag :search, "Search Services" %>
  <%= text_field_tag :search, params[:search], placeholder: "Search by title or content" %>
  <%= submit_tag "Search" %>
<% end %>

<h1>Articles</h1>
<ul>
  <% @services.each do |article| %>
    <li><%= service.title %></li>
  <% end %>
</ul>
